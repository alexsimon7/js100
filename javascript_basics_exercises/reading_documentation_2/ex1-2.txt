1.

let iceCreamTaste = 'chocolate';
let iceCreamDensity = 10;

while (iceCreamDensity > 0) {
  console.log('Drip...');
  iceCreamDensity -= 1;
}

console.log(`The '${iceCreamTaste}' ice cream melted.`);

2. Primative data types = 'strings', 'numbers', 'booleans'. Missed 'null', 'undefined', 'symbol' and 'BigInt' types from
   memory.



