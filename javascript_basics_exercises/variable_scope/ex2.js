console.log(greeting);

let greeting = 'Hello world';

//answer: The above code will throw an error because the variable 'greeting' is referenced before it was declared. Unlike 'var',
//        'let' does not hoist the variable.
